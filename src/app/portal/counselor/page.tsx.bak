"use client";

import { useState, useEffect } from "react";
import { useUser } from "@clerk/nextjs";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { Progress } from "@/components/ui/progress";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import {
  Users,
  TrendingUp,
  BookOpen,
  Target,
  Calendar,
  Award,
  Settings,
  CheckCircle2,
  AlertCircle,
  Brain,
  Heart,
  Lightbulb,
  GraduationCap,
  Sparkles,
  ArrowRight,
  Flame,
  MessageSquare,
  Video,
  Phone,
  Mail,
  Clock,
  FileText,
  BarChart3,
  UserCheck,
  Calendar as CalendarIcon,
  Search,
  Filter,
  Send,
  Bell,
  Eye,
  Download,
  Plus,
  X,
  AlertTriangle,
  RefreshCw,
  Mail as MailIcon,
  UserMinus,
  UserPlus,
  Building2,
  Briefcase,
} from "lucide-react";
import Link from "next/link";
import { INDUSTRIES, OUTREACH_TEMPLATES } from "@/lib/industry-database";

export default function CounselorPortalPage() {
  const { user } = useUser();
  const [loading, setLoading] = useState(true);

  // Mock counselor data
  const [counselorData, setCounselorData] = useState({
    name: "Dorji Wangmo",
    school: "Yangchenphug HSS",
    studentsAssigned: 250,
    sessionsThisMonth: 48,
    satisfactionRate: 94,
  });

  // Upcoming sessions
  const [upcomingSessions, setUpcomingSessions] = useState([
    {
      id: 1,
      student: "Tashi Dorji",
      class: "10 A",
      type: "career-guidance",
      time: "10:00 AM",
      date: "Today",
      topic: "RUB College Selection",
      status: "confirmed",
    },
    {
      id: 2,
      student: "Karma Choden",
      class: "12 Science",
      type: "assessment-review",
      time: "2:30 PM",
      date: "Today",
      topic: "RIASEC Results Discussion",
      status: "pending",
    },
    {
      id: 3,
      student: "Sonam Wangyal",
      class: "11 Commerce",
      type: "study-abroad",
      time: "11:00 AM",
      date: "Tomorrow",
      topic: "Australia Scholarship Options",
      status: "confirmed",
    },
  ]);

  // Students needing attention
  const [attentionNeeded, setAttentionNeeded] = useState([
    {
      id: 1,
      name: "Pema Lhamo",
      class: "12 Science",
      reason: "Low confidence in career choice",
      priority: "high",
      lastContact: "2 weeks ago",
    },
    {
      id: 2,
      name: "Jigme Norbu",
      class: "10 B",
      reason: "Parent expectation mismatch",
      priority: "medium",
      lastContact: "1 week ago",
    },
    {
      id: 3,
      name: "Dechen Wangmo",
      class: "12 Arts",
      reason: "Scholarship application deadline approaching",
      priority: "high",
      lastContact: "3 days ago",
    },
  ]);

  // School-wide stats
  const [schoolStats, setSchoolStats] = useState({
    totalStudents: 450,
    assessmentsCompleted: 320,
    careerPathsIdentified: 280,
    atRiskStudents: 25,
    avgEngagement: 78,
  });

  // Recent activities
  const [recentActivities] = useState([
    { type: "session", title: "Completed career guidance session with Tashi", time: "2 hours ago", icon: MessageSquare },
    { type: "assessment", title: "Reviewed 5 student assessment results", time: "5 hours ago", icon: FileText },
    { type: "workshop", title: "Conducted Study Abroad workshop", time: "Yesterday", icon: Video },
    { type: "meeting", title: "Parent-teacher meeting for Class 12", time: "2 days ago", icon: Users },
  ]);

  // Quick actions
  const [resources, setResources] = useState([
    { title: "Counseling Toolkit", count: 24, icon: BookOpen, href: "#" },
    { title: "Career Database", count: 150, icon: GraduationCap, href: "/dashboard/careers" },
    { title: "Scholarship Listings", count: 45, icon: Award, href: "/dashboard/scholarships" },
    { title: "Assessment Templates", count: 12, icon: FileText, href: "/dashboard/assessment" },
  ]);

  useEffect(() => {
    setTimeout(() => setLoading(false), 1000);
  }, []);

  if (loading) {
    return (
      <div className="min-h-screen bg-gradient-to-br from-teal-50 via-blue-50 to-indigo-50 flex items-center justify-center">
        <div className="text-center">
          <div className="w-16 h-16 border-4 border-teal-600 border-t-transparent rounded-full animate-spin mx-auto mb-4" />
          <p className="text-gray-600">Loading Counselor Portal...</p>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-gradient-to-br from-teal-50 via-blue-50 to-indigo-50">
      {/* Header */}
      <div className="bg-gradient-to-r from-teal-600 via-blue-600 to-indigo-600 text-white">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
          <div className="flex items-center justify-between">
            <div className="flex items-center gap-4">
              <div className="w-16 h-16 bg-white/20 rounded-full flex items-center justify-center">
                <MessageSquare className="w-8 h-8" />
              </div>
              <div>
                <h1 className="text-2xl md:text-3xl font-bold">Career Counselor Portal</h1>
                <p className="text-teal-100">{counselorData.school}</p>
              </div>
            </div>
            <div className="flex items-center gap-3">
              <div className="text-center px-4 py-2 bg-white/10 rounded-lg">
                <Users className="w-6 h-6 mx-auto text-teal-200" />
                <p className="text-sm font-medium">{counselorData.studentsAssigned}</p>
                <p className="text-xs text-teal-100">Students</p>
              </div>
              <div className="text-center px-4 py-2 bg-white/10 rounded-lg">
                <Sparkles className="w-6 h-6 mx-auto text-yellow-300" />
                <p className="text-sm font-medium">{counselorData.satisfactionRate}%</p>
                <p className="text-xs text-teal-100">Satisfaction</p>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Quick Stats */}
        <div className="grid md:grid-cols-5 gap-6 mb-8">
          <Card className="bg-gradient-to-br from-teal-500 to-teal-600 text-white border-0">
            <CardContent className="pt-6">
              <div className="flex items-center justify-between">
                <div>
                  <p className="text-teal-100 text-sm">Total Students</p>
                  <p className="text-3xl font-bold">{schoolStats.totalStudents}</p>
                </div>
                <Users className="w-10 h-10 text-teal-200" />
              </div>
            </CardContent>
          </Card>

          <Card className="bg-gradient-to-br from-blue-500 to-blue-600 text-white border-0">
            <CardContent className="pt-6">
              <div className="flex items-center justify-between">
                <div>
                  <p className="text-blue-100 text-sm">Assessments Done</p>
                  <p className="text-3xl font-bold">{schoolStats.assessmentsCompleted}</p>
                </div>
                <Target className="w-10 h-10 text-blue-200" />
              </div>
            </CardContent>
          </Card>

          <Card className="bg-gradient-to-br from-indigo-500 to-indigo-600 text-white border-0">
            <CardContent className="pt-6">
              <div className="flex items-center justify-between">
                <div>
                  <p className="text-indigo-100 text-sm">Career Paths Set</p>
                  <p className="text-3xl font-bold">{schoolStats.careerPathsIdentified}</p>
                </div>
                <TrendingUp className="w-10 h-10 text-indigo-200" />
              </div>
            </CardContent>
          </Card>

          <Card className="bg-gradient-to-br from-orange-500 to-orange-600 text-white border-0">
            <CardContent className="pt-6">
              <div className="flex items-center justify-between">
                <div>
                  <p className="text-orange-100 text-sm">Sessions This Month</p>
                  <p className="text-3xl font-bold">{counselorData.sessionsThisMonth}</p>
                </div>
                <Calendar className="w-10 h-10 text-orange-200" />
              </div>
            </CardContent>
          </Card>

          <Card className="bg-gradient-to-br from-red-500 to-red-600 text-white border-0">
            <CardContent className="pt-6">
              <div className="flex items-center justify-between">
                <div>
                  <p className="text-red-100 text-sm">Need Attention</p>
                  <p className="text-3xl font-bold">{schoolStats.atRiskStudents}</p>
                </div>
                <AlertCircle className="w-10 h-10 text-red-200" />
              </div>
            </CardContent>
          </Card>
        </div>

        <div className="grid lg:grid-cols-3 gap-8">
          {/* Main Content */}
          <div className="lg:col-span-2 space-y-6">
            {/* Upcoming Sessions */}
            <Card>
              <CardHeader>
                <div className="flex items-center justify-between">
                  <CardTitle className="flex items-center gap-2">
                    <Clock className="w-5 h-5 text-teal-600" />
                    Upcoming Sessions
                  </CardTitle>
                  <Button variant="outline" size="sm" asChild>
                    <Link href="/calendar">View Calendar</Link>
                  </Button>
                </div>
                <CardDescription>Your scheduled counseling sessions</CardDescription>
              </CardHeader>
              <CardContent>
                <div className="space-y-3">
                  {upcomingSessions.map((session) => (
                    <div key={session.id} className="flex items-center justify-between p-4 bg-gray-50 rounded-lg hover:bg-gray-100 transition-colors">
                      <div className="flex items-center gap-4">
                        <div className={`w-12 h-12 rounded-full flex items-center justify-center ${
                          session.type === "career-guidance" ? "bg-blue-100 text-blue-600" :
                          session.type === "assessment-review" ? "bg-purple-100 text-purple-600" :
                          "bg-green-100 text-green-600"
                        }`}>
                          {session.type === "career-guidance" ? <GraduationCap className="w-6 h-6" /> :
                           session.type === "assessment-review" ? <FileText className="w-6 h-6" /> :
                           <Video className="w-6 h-6" />}
                        </div>
                        <div>
                          <p className="font-medium">{session.student}</p>
                          <p className="text-sm text-gray-500">{session.class} • {session.topic}</p>
                        </div>
                      </div>
                      <div className="text-right">
                        <p className="font-medium text-sm">{session.date}</p>
                        <p className="text-xs text-gray-500">{session.time}</p>
                        <Badge className={
                          session.status === "confirmed" ? "bg-green-100 text-green-800" : "bg-yellow-100 text-yellow-800"
                        } variant="outline">
                          {session.status}
                        </Badge>
                      </div>
                    </div>
                  ))}
                </div>
              </CardContent>
            </Card>

            {/* Students Needing Attention */}
            <Card className="border-2 border-orange-200">
              <CardHeader>
                <CardTitle className="flex items-center gap-2">
                  <AlertCircle className="w-5 h-5 text-orange-600" />
                  Students Requiring Attention
                </CardTitle>
                <CardDescription>Priority follow-ups needed</CardDescription>
              </CardHeader>
              <CardContent>
                <div className="space-y-3">
                  {attentionNeeded.map((student) => (
                    <div key={student.id} className="flex items-center justify-between p-4 bg-orange-50 rounded-lg">
                      <div className="flex items-center gap-4">
                        <div className="w-12 h-12 bg-orange-200 rounded-full flex items-center justify-center text-orange-700 font-bold">
                          {student.name.split(" ").map((n: string) => n[0]).join("")}
                        </div>
                        <div>
                          <p className="font-medium">{student.name}</p>
                          <p className="text-sm text-gray-500">{student.class}</p>
                          <p className="text-xs text-orange-600">{student.reason}</p>
                        </div>
                      </div>
                      <div className="flex items-center gap-3">
                        <Badge className={
                          student.priority === "high" ? "bg-red-100 text-red-800" : "bg-yellow-100 text-yellow-800"
                        }>
                          {student.priority}
                        </Badge>
                        <div className="flex gap-2">
                          <Button size="sm" variant="outline">
                            <Phone className="w-4 h-4 mr-1" />
                            Call
                          </Button>
                          <Button size="sm">
                            <MessageSquare className="w-4 h-4 mr-1" />
                            Message
                          </Button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              </CardContent>
            </Card>

            {/* Recent Activity */}
            <Card>
              <CardHeader>
                <CardTitle className="flex items-center gap-2">
                  <Calendar className="w-5 h-5 text-blue-600" />
                  Recent Activity
                </CardTitle>
              </CardHeader>
              <CardContent>
                <div className="space-y-4">
                  {recentActivities.map((activity, index) => (
                    <div key={index} className="flex items-start gap-4 p-4 bg-gray-50 rounded-lg">
                      <div className="w-10 h-10 bg-blue-100 rounded-full flex items-center justify-center flex-shrink-0">
                        <activity.icon className="w-5 h-5 text-blue-600" />
                      </div>
                      <div className="flex-1">
                        <p className="font-medium">{activity.title}</p>
                        <p className="text-sm text-gray-500">{activity.time}</p>
                      </div>
                    </div>
                  ))}
                </div>
              </CardContent>
            </Card>
          </div>

          {/* Sidebar */}
          <div className="space-y-6">
            {/* Quick Actions */}
            <Card>
              <CardHeader>
                <CardTitle>Quick Actions</CardTitle>
              </CardHeader>
              <CardContent className="space-y-3">
                <Button className="w-full justify-start" variant="outline" asChild>
                  <Link href="/dashboard/assessment">
                    <Target className="w-4 h-4 mr-2" />
                    Schedule Assessment
                  </Link>
                </Button>
                <Button className="w-full justify-start" variant="outline" asChild>
                  <Link href="/calendar">
                    <CalendarIcon className="w-4 h-4 mr-2" />
                    Book Session
                  </Link>
                </Button>
                <Button className="w-full justify-start" variant="outline" asChild>
                  <Link href="/dashboard/careers">
                    <GraduationCap className="w-4 h-4 mr-2" />
                    Career Database
                  </Link>
                </Button>
                <Button className="w-full justify-start" variant="outline" asChild>
                  <Link href="/dashboard/study-abroad">
                    <Video className="w-4 h-4 mr-2" />
                    Study Abroad Info
                  </Link>
                </Button>
              </CardContent>
            </Card>

            {/* Resources */}
            <Card>
              <CardHeader>
                <CardTitle className="text-lg">Resources</CardTitle>
              </CardHeader>
              <CardContent>
                <div className="grid grid-cols-2 gap-3">
                  {resources.map((resource, index) => (
                    <Link key={index} href={resource.href} className="p-3 bg-gray-50 rounded-lg hover:bg-gray-100 transition-colors text-center">
                      <resource.icon className="w-6 h-6 mx-auto mb-2 text-teal-600" />
                      <p className="text-sm font-medium">{resource.title}</p>
                      <p className="text-xs text-gray-500">{resource.count} items</p>
                    </Link>
                  ))}
                </div>
              </CardContent>
            </Card>

            {/* Engagement Overview */}
            <Card>
              <CardHeader>
                <CardTitle className="text-lg flex items-center gap-2">
                  <BarChart3 className="w-5 h-5 text-teal-600" />
                  School Engagement
                </CardTitle>
              </CardHeader>
              <CardContent>
                <div className="text-center mb-4">
                  <div className="text-4xl font-bold text-teal-600">{schoolStats.avgEngagement}%</div>
                  <p className="text-sm text-gray-600">Average Student Engagement</p>
                </div>
                <Progress value={schoolStats.avgEngagement} className="h-2 mb-4" />
                <div className="space-y-2 text-sm">
                  <div className="flex justify-between">
                    <span>Assessments Completed</span>
                    <span className="font-medium">{Math.round((schoolStats.assessmentsCompleted / schoolStats.totalStudents) * 100)}%</span>
                  </div>
                  <div className="flex justify-between">
                    <span>Career Paths Identified</span>
                    <span className="font-medium">{Math.round((schoolStats.careerPathsIdentified / schoolStats.totalStudents) * 100)}%</span>
                  </div>
                </div>
              </CardContent>
            </Card>

            {/* Contact Support */}
            <Card className="bg-gradient-to-br from-teal-600 to-blue-600 text-white border-0">
              <CardContent className="pt-6">
                <MessageSquare className="w-8 h-8 mb-3" />
                <h3 className="font-semibold mb-2">Need Support?</h3>
                <p className="text-sm text-teal-100 mb-4">
                  Connect with other counselors or access training resources.
                </p>
                <div className="space-y-2">
                  <Button variant="secondary" size="sm" className="w-full justify-start">
                    <Mail className="w-4 h-4 mr-2" />
                    Email Support
                  </Button>
                  <Button variant="secondary" size="sm" className="w-full justify-start">
                    <Video className="w-4 h-4 mr-2" />
                    Training Videos
                  </Button>
                </div>
              </CardContent>
            </Card>
          </div>
        </div>
      </div>
    </div>
  );
}
